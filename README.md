# Isso é Automatizável 🧠⚙️

Uma ferramenta de **element picker** para web — permite selecionar elementos na página com o mouse e copiar um selector inteligente e reutilizável para automação (RPA, testes, scraping, etc).

---

## 📌 O que é este projeto

`Isso é Automatizável` é um seletor visual de elementos DOM.  
Quando ativado, ele destaca elementos com o cursor e, ao clicar, copia para a área de transferência um selector **robusto e específico**, com suporte a `data-*`, `id`, `name` e classes.

Esse selector pode ser usado em automação de testes, ferramentas RPA ou qualquer script que precise apontar elementos de forma confiável.

---

## 🧠 Funcionalidades principais

✔️ Modo de seleção visual  
✔️ Highlight em hover  
✔️ Geração de selectors intuitivos  
✔️ Copia selector para a área de transferência  
✔️ Funciona via click ou menu de contexto (clique direito)
